# C4 Architecture -- json-yaml-spark-0606

## Architectural Intent

The app is a fully static browser application. All parsing, validation, conversion, and formatting happen locally in the user's browser. This keeps the system simple, privacy-preserving, and aligned with the utility-tool use case described in the [PRD](../prd/project-prd.md).

## Level 1: System Context

```mermaid
graph TD
    User["User<br/>developer, operator, learner"]
    App["JSON ⇄ YAML Spark<br/>static conversion web app"]
    Host["Static hosting / CDN<br/>serves HTML, CSS, JS"]
    Clipboard["Browser Clipboard API<br/>copies converted output"]
    Storage["Browser Local Storage<br/>persists indentation preferences"]

    User -->|"paste, validate, convert, copy"| App
    Host -->|"delivers app shell"| App
    App -->|"write copied output"| Clipboard
    App -->|"read/write preferences"| Storage
```

## Level 2: Container Diagram

```mermaid
graph TD
    User["User"]

    subgraph System["JSON ⇄ YAML Spark"]
        UI["Browser UI Container<br/>static SPA<br/>renders workspace, controls, examples"]
        Logic["Conversion Logic Container<br/>parser + formatter modules<br/>validates and transforms text"]
        State["Client State Container<br/>in-memory state + local persistence<br/>tracks preferences and validation state"]
    end

    Static["Static asset host<br/>CDN or static site platform"]
    Clipboard["Clipboard API"]
    Storage["Local Storage"]

    Static -->|"HTML/CSS/JS"| UI
    User -->|"HTTPS"| UI
    UI -->|"invoke conversion"| Logic
    UI -->|"read/write app state"| State
    Logic -->|"normalized result/error"| UI
    State -->|"persist preferences"| Storage
    UI -->|"copy output"| Clipboard
```

## Level 3: Component Diagram

```mermaid
graph TD
    subgraph BrowserApp["Browser UI Container"]
        Shell["App Shell<br/>bootstraps layout and restored preferences"]
        Toolbar["Direction + settings toolbar<br/>format toggle, indentation controls, examples trigger"]
        Source["Source Editor Panel<br/>input textarea, validation summary"]
        Output["Output Panel<br/>formatted result, copy action"]
        Errors["Error Guidance Panel<br/>plain-language explanation + raw details"]
        Examples["Examples Catalog<br/>local bundled snippets"]
        Store["Preferences and Session Store<br/>current direction, indentation, validation state"]
        Service["Converter Service Adapter<br/>coordinates parse, convert, format"]
        ClipboardComp["Copy Controller<br/>wraps clipboard interactions"]
    end

    Shell --> Toolbar
    Shell --> Source
    Shell --> Output
    Shell --> Errors
    Toolbar --> Store
    Toolbar --> Examples
    Source --> Store
    Source --> Service
    Service --> Output
    Service --> Errors
    Output --> ClipboardComp
    Store --> Shell
```

## Boundaries and Integration Notes

- There is no backend application boundary in v1.
- The static host is replaceable infrastructure and does not hold business logic.
- Parser/formatter libraries are implementation details behind the converter service adapter.
- Browser platform integrations are limited to clipboard and local preference persistence.

## Failure and Recovery Model

- Parse failure stays local and produces a normalized UI error state.
- Copy failure is non-destructive; output remains present and retryable.
- Corrupt saved preferences fall back to safe defaults rather than blocking app startup.
